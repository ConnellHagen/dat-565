You should create a python virtual environment before running this for best results
`mkdir .venv && python -m venv .venv`

## Dependencies:
`python -m pip install numpy`
`python -m pip install pandas`
`python -m pip install matplotlib`